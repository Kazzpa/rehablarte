name: ruff
on: [push, pull_request]
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
    - uses: astral-sh/ruff-action@v3
      with:
        src: "./src"
    - run: ruff check --fix
    - run: ruff format
